export interface UserAnonymous {
    id: number,
    name: string | null,
    identification: string | null,
    phone: string | null,
    age: number | null,
    gender: string | null,
    height: number | null
}
